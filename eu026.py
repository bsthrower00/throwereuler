# # reciprocal cycles
longest=0
# for i in range(3,10):
    # p = 1/i
    # p = str(p)[2:-1]
    # s = {}
# p = str(1/7)[2:-1]
# s = set({})
# for q in range(len(p)):
    # s2 = {p[:q]}
    # if s2.issubset(s) is True:
        # break
    # else:
        # s = s.union(s2)
# print(s)        
def longDiv(numer,div,quot=0):
    num = str(numer)
    if 
    a = div // num[0]